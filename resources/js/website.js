import $ from 'jquery';
import { 
    createIcons, 
    Globe,
} from 'lucide';

document.addEventListener('DOMContentLoaded', () => {
    createIcons({
      icons: {
        Globe,
      }
    });
});

$(function () {
    //
});
